# 2026-06-20 Codex

## Codex - STT layer implementation
What I worked on:
- Implemented the portable Deepgram speech-to-text layer in `backend/api`.
- Added a CLI path so the team can transcribe local demo audio before the frontend is wired up.
- Added regression tests to lock the future-facing API/CLI contract.

What changed:
- Added FastAPI routes: `GET /health` and `POST /api/transcriptions`.
- Added reusable transcription modules for Deepgram HTTP calls, response normalization, typed errors, schemas, and CLI usage.
- Added `backend/api/contracts/transcription_response.example.json` as the stable response shape for future Next.js work.
- Added `backend/api/.env.example` and `backend/api/requirements.txt`.
- Updated `backend/api/README.md` with setup, CLI usage, API usage, and curl examples.
- Added pytest coverage for Deepgram fixture normalization, API contract, CLI output, missing API key, provider failure, and upload field/route stability.

What's next / blockers:
- Add `DEEPGRAM_API_KEY` locally before calling the live Deepgram API.
- Test with `demo/voicemails/University of California, Berkeley.m4a` using:
  `cd backend/api && source .venv/bin/activate && python -m transcription.cli "../../demo/voicemails/University of California, Berkeley.m4a" --pretty`
- Decide whether Next.js should proxy this FastAPI service or port the same `/api/transcriptions` contract into a TypeScript route.
