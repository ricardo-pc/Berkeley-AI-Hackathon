# 2026-06-20 Codex

## Codex - STT layer implementation
What I worked on:
- Implemented the portable Deepgram speech-to-text layer in `backend/api`.
- Added a CLI path so the team can transcribe local demo audio before the frontend is wired up.
- Added regression tests to lock the future-facing API/CLI contract.

What changed:
- Added FastAPI routes: `GET /health` and `POST /api/transcriptions`.
- Added reusable transcription modules for Deepgram HTTP calls, response normalization, typed errors, schemas, and CLI usage.
- Added `backend/api/contracts/transcription_response.example.json` as the stable response shape for future Next.js work.
- Added `backend/api/.env.example` and `backend/api/requirements.txt`.
- Updated `backend/api/README.md` with setup, CLI usage, API usage, and curl examples.
- Added pytest coverage for Deepgram fixture normalization, API contract, CLI output, missing API key, provider failure, and upload field/route stability.

What's next / blockers:
- Add `DEEPGRAM_API_KEY` locally before calling the live Deepgram API.
- Test with any local demo audio file using:
  `cd backend/api && source .venv/bin/activate && python -m transcription.cli "../../demo/voicemails/<your-audio-file>.m4a" --pretty`
- Ported the same `/api/transcriptions` contract into the Next.js app under `berkapp/app/api/transcriptions/route.ts`.
- Next step: build the frontend upload/recording UI that calls the Next.js API route.

## Codex - Intake Agent implementation
What I worked on:
- Implemented the Intake Agent in `agents/intake` as a standalone Claude-backed extractor over Deepgram/STT JSON.

What changed:
- Added strict intake output fields aligned with the DB docs: `first_name`, `last_name`, `date_of_birth`, `phone_number`, `insurance_plan`, nested `request`, `missing_fields`, and `transcript`.
- Moved request-related fields under `request`: `type`, `details`, `orders`, `preferred_times`, and `urgency_signal`.
- Added CLI usage: `python3 -m agents.intake.cli path/to/stt-output.json --pretty`.
- Added tests for transcript extraction from normalized/raw Deepgram payloads, Claude JSON parsing, stable missing-key/error handling, service orchestration, and CLI output.

What's next / blockers:
- Needs `ANTHROPIC_API_KEY` configured before live Claude extraction works.
- After generating a real Deepgram transcription JSON, feed it into the Intake Agent CLI and compare the extracted fields against the seeded patients in `docs/database/README.md`.
