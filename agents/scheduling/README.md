# Scheduling Agent

Finds the right slot given: provider availability, visit type duration, patient preference, urgency weighting.
Actual scheduling logic lives here, not just an LLM call.

Needs calendar access — Google Calendar? TBD.
